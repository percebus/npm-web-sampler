apiVersion: v1
kind: Service
metadata:
  name: {{ include "npm-web-sampler.fullname" . }}
  labels:
    {{- include "npm-web-sampler.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "npm-web-sampler.selectorLabels" . | nindent 4 }}
